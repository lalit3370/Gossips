import React from "react";
import { useHistory } from "react-router-dom";
import Asynchronous from "./SearchAutoComplete";

export default function SearchBoard() {
    return(
            <Asynchronous/>
    )
}
